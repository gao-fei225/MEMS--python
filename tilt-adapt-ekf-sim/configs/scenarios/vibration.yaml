# 振动工况配置
# a_lin_n 带限随机（RMS、带宽）

name: vibration
fs: 100
duration_s: 30

# 姿态（固定）
roll_deg: 2
pitch_deg: -1
yaw_deg: 0

# 振动参数
vib_rms: 0.5          # m/s^2 RMS
vib_bandwidth_hz: 10  # 带宽 (Hz)
vib_center_hz: 0      # 中心频率 (Hz)，0 表示低通

temp_C: 25
seed: 1
